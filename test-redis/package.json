{
  "name": "test-redis",
  "scripts": {
    "start": "concurrently --kill-others './start-local-redis.sh' 'wait-on tcp:6379 && redis-commander'"
  },
  "dependencies": {
    "concurrently": "^3.4.0",
    "redis-commander": "^0.4.3",
    "wait-on": "^2.0.2"
  }
}
